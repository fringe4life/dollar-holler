## Changes Made

### 1. Fixed margin collapsing & empty nav space — `Navbar.svelte`
- **File:** `src/lib/components/navbar/Navbar.svelte`
- **Lines changed:** ~89–95 (nav element's `css({...})`)
- **Changes:**
  - Added `display: "flex"` and `flexDirection: "column"` to the nav — establishes a flex formatting context which prevents parent↔child margin collapsing (the logo div's `marginBlock` was escaping the nav on `md` where `position` is `static`)
  - Replaced `minBlockSize: "100dvh"` with `blockSize: { md: "100%" }` — on mobile, `position: fixed; inset: 0` already sizes the nav to the full viewport so `minBlockSize` was redundant; on `md` (static, in-flow), `height: 100%` makes the nav fill the header grid cell completely, eliminating the empty `daisyBush` background that appeared below the nav links when the page content grew taller than 100dvh
- **Revert:** Remove `display: "flex"` and `flexDirection: "column"`; replace `blockSize: { md: "100%" }` with `minBlockSize: "100dvh"`

### 2. Fixed navbar scrolling — `Navbar.svelte`
- **File:** `src/lib/components/navbar/Navbar.svelte`
- **Changes:**
  - `position: { base: "fixed", md: "static" }` → `position: { base: "fixed", md: "sticky" }` — nav now sticks to the viewport top on desktop while main content scrolls
  - `blockSize: { md: "100%" }` → `blockSize: { md: "100dvh" }` — nav occupies exactly the viewport height as a sticky sidebar (inset: 0 already covers top: 0 for the sticky threshold)
- **Revert:** Change `sticky` back to `static`, change `100dvh` back to `100%`

## Revert Status
- [ ] Change 1 — flex + blockSize fix in Navbar.svelte
- [ ] Change 2 — sticky position + 100dvh
